[project]
name = "zotero-rag-embeddings"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "litellm",
    "python-dotenv",
    "tenacity",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
include = ["embed.py"]
