[project]
name = "zotero-rag-ingest"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "polars",
    "python-dotenv",
    "loguru",
    "docling",
    "lancedb",
    "httpx",
    "tqdm",
]

[tool.uv]
package = false
