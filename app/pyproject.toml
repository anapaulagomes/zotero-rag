[project]
name = "zotero-rag-app"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "chainlit",
    "lancedb",
    "httpx",
    "litellm",
    "python-dotenv",
    "loguru",
    "polars",
    "tenacity",
]

[tool.uv]
package = false
